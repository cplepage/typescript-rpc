{
  "scripts": {
    "build": "esbuild client.ts --outfile=client.js --bundle --minify",
    "start": "npm run build && ts-node server"
  },
  "dependencies": {
    "@types/node": "^18.11.18",
    "esbuild": "^0.17.3",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.4",
    "typescript-rpc": "^0.0.10"
  }
}
