{
  "scripts": {
    "build": "esbuild client.ts --outfile=client.js --bundle --minify & esbuild server.ts --outfile=server.mjs",
    "start": "npm run build && node server.mjs"
  },
  "dependencies": {
    "@types/node": "^18.11.18",
    "esbuild": "^0.17.5",
    "typescript-rpc": "^0.0.5"
  }
}
